import { SubItem } from './SubItem'

export const SidebarSubscription = () => {
	return (
		<div>
			<ul>
				{/* {menu.map(menuItem => (
					<SubItem
						key={menuItem.label}
						item={menuItem}
					/>
				))} */}
			</ul>
		</div>
	)
}
